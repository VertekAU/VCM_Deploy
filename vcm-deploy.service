[Unit]
Description=VCM Deploy — device provisioning and identity
After=vcm-modem-reconnect.service
# Reboot on failure when unprovisioned — see condition in vcm-failure-reboot.service
OnFailure=vcm-failure-reboot.service
# 10 attempts at 30s intervals (~5 min) before entering failed state
StartLimitBurst=10
StartLimitIntervalSec=600

[Service]
Type=oneshot
RemainAfterExit=yes
ExecStart=/usr/local/sbin/vcm_deploy.sh
TimeoutStartSec=600
StandardOutput=journal
StandardError=journal
Restart=on-failure
RestartSec=30

[Install]
WantedBy=multi-user.target
